node_modules
.pi/todos
*.profraw
# 覆盖 .pi 目录及指向其它 worktree 的 .pi 符号链接（.pi/ 仅匹配目录，不匹配符号链接）
.pi
.vscode/
.DS_Store
test/results/
openspec/

# bench — 评测运行时产物与缓存
bench/results/
bench/.cache/
bench/.smoke/
bench/.ruff_cache/

# pi-lens — runtime caches and work state (regenerated automatically)
.pi-lens/

# Sync tool — runtime log (regenerated automatically)
scripts/sync-to-local-pi.log

.worktrees/

# 本地 linked worktree（如 wt/Virgo-Spica，分支 wt/tui-design）
wt/

**/dist/

# 临时设计/规划产物
.scratch/

# Spurious self-referencing symlinks from npm link in extension packages
extensions/meta/pi-logger/pi-logger
extensions/meta/pi-config/pi-config
extensions/meta/selector/selector

# e2e 夹具：preset-home 故意内嵌 .pi 目录结构（预置扩展配置），需要提交
!test/e2e/extensions/custom-rename/preset-home/.pi/
